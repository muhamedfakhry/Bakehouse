pipeline {
    agent {label "sys-sdmin-mnf"}

    stages {
        stage('build') {
            steps {
                echo 'build'
                sh "ls"
            }
        }
        stage('test') {
            steps {
                echo 'test'
                sh "ls"
            }
        }
        stage('quality') {
            steps {
                echo 'quality'
                sh "ls"
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy'
                sh "ls"
            }
        }
    }
}
